all:
  vars:
    ansible_user: azureuser
    ansible_ssh_private_key_file: ~/.ssh/id_rsa_azure
    ansible_python_interpreter: /usr/bin/python3
  children:
    web:
      hosts:
        vm-web-bb-dev:
          ansible_host: 10.20.2.4
    api:
      hosts:
        vm-api-bb-dev:
          ansible_host: 10.20.3.4
    ops:
      hosts:
        vm-ops-bb-dev:
          ansible_host: 10.20.4.4

