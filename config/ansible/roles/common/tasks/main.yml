---
- name: Install common Linux packages
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - python3
      - python3-pip
      - unzip
    state: present
    update_cache: true

